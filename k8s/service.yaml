apiVersion: v1
kind: Service
metadata:
  name: lms-api-service
  labels:
    app: lms-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: lms-api

