apiVersion: v1
kind: Secret
metadata:
  name: lms-secret
  labels:
    app: lms-api
type: Opaque
stringData:
  DB_PASSWORD: "lms_password"  # Change this in production!
  SECRET_KEY: "replace_this_with_real_secret"  # Change this in production!

